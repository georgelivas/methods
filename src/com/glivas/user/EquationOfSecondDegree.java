package com.glivas.user;


public class EquationOfSecondDegree {
    public static int EquationOfSecondDegree (int a, int b, int c) {
       double delta = 0;
        if(methods.isZero(a) == false) {
          delta = methods.delta(a, b, c);
        }
        if (methods.isPositive(delta)){

        }
    }
}
